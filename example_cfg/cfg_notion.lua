dopath("mod_notionflux") -- socket-based notion ipc
dopath("mod_tiling")     -- static tiling notion module
dopath("mod_xrandr")     -- xrandr support for notion
dopath("mod_dock")       -- dock support for notion
dopath("mod_xkbevents")  -- xkbevents support

dopath("dbg")               -- functions for stderr debbuging
dopath("cfg_settings")      -- notioncore.set
dopath("cfg_rules")         -- window placement rules
dopath("cfg_layouts")       -- a bunch of default layouts
dopath("app")               -- module to jump by class tag instance etc
dopath("named_scratchpad")  -- named scratchpad module
dopath("hide_tabs")         -- hide tabs
dopath("net_client_list")   -- net client list to provide `wmctrl -l`
dopath("directions")        -- 2bwm-like directions
dopath("screenshot")        -- screenshot script
dopath("cfg_dzen")          -- dzen config
dopath("rofi")              -- rofi module for menu
dopath("cfg_autostart")     -- apps autostart
dopath("functions")         -- helpers. Primarily for cfg_kb.lua
dopath("cfg_kb")            -- keybindings
dopath("cfg_xrandr")        -- xrandr config
dopath("transparency")      -- autoset transparency
dopath("dzen_bg")           -- background reader for dzen
dopath("lua_repl")          -- experimental script standalone lua repl
